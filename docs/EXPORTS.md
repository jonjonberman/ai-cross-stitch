# Exports

## Implemented

- JSON export (`exportJson.ts`).
- CSV grid export (`exportCsv.ts`).
- Preview export placeholder via PPM renderer (`exportPng.ts`).

## Planned

- True PNG rendering via Sharp.
- PDF stitch chart export with legend and symbol pages.
